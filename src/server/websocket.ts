// WebSocket server stub -- requires ioredis and limiter packages.
// This standalone WebSocket server is not used in the current deployment.
// The app uses the socket.io client stub in src/lib/socket.ts instead.
//
// To enable: install ioredis, limiter, and configure REDIS_URL env var.

console.log('[WEBSOCKET STUB] WebSocket server not started -- dependencies not installed');

export {};
